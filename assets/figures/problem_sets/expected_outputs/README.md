# Expected Outputs (Generated)

This directory contains generated media files (e.g. PNGs) for "expected output"
blocks shown on the rendered problem set pages.

The files are created by `scripts/generate_student_notebooks.py` and are
intentionally **not** tracked in git.

