# Student Notebook Downloads (Generated)

This directory contains the generated **clean** problem set notebooks that students
download (e.g. `pset_1.ipynb`).

They are generated by `scripts/generate_student_notebooks.py` and are intentionally
**not** tracked in git.

The corresponding **rich** notebooks that are used to render the website pages live in:
`chapters/problem_sets/`.
